import styled from 'styled-components/macro';
import { Button } from 'components/button/styles';

export const Filter = styled.div`
  width: 100%;
  overflow: auto;
  white-space: nowrap;
`;

export const FilterItemLink = styled(Button)``;
